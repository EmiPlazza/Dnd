import React, { useState, useEffect } from 'react';
import { View, SafeAreaView,TouchableOpacity, Text, StyleSheet , RefreshControl, ScrollView, ImageBackground} from 'react-native';
import  ControllerHoja  from '../controllers/ControllerHoja';



const ViewListado = ({ navigation }) => {
  return (
    <SafeAreaView>
       <Text>Pagina de listado</Text>
    </SafeAreaView>
  );
};

export default ViewListado;
