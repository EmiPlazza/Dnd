import React from 'react';
import {Text, StyleSheet} from 'react-native'; 
import { useTheme } from 'react-native-paper';



const StyleHoja = () => {
    const theme = useTheme(); 

    const styles = StyleSheet.create({
     
});

    return styles; 
};

export default  StyleHoja